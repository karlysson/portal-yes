.col-xs-12
  .box.box-primary
    .box-header.with-border
      h3.box-title Categoria de Empresas
      .box-body
       = simple_form_for([:admin, @company_category]) do |f|
        = f.error_notification

        .form-inputs
          = f.input :name
        .form-actions
          = f.button :submit, :class => "btn btn-success"

  a.btn.btn-primary href="/admin/company_categories"  Voltar
